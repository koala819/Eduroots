---
# Source: netdata/templates/parent/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: netdata
  namespace: default
  labels:
    app: netdata
    chart: netdata-3.7.141
    release: netdata
    heritage: Helm
    role: parent
  annotations:
spec:
  type: ClusterIP
  ports:
    - port: 19999
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: netdata
    release: netdata
    role: parent
